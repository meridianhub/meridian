{
    "$schema": "https://cookbook.meridianhub.org/schema/registry-item.json",
    "name": "saga-timeline",
    "type": "registry:ui",
    "title": "Saga Timeline",
    "description": "Visualises the lifecycle of a Meridian saga as a horizontal step timeline, showing INITIATED, RESERVED, EXECUTING, and COMPLETED states with timestamps. Renders a separate compensation branch when rollback steps are present.",
    "categories": ["saga", "timeline", "display", "status"],
    "files": [
        {
            "path": "frontend/src/components/shared/saga-timeline.tsx",
            "type": "registry:ui"
        }
    ],
    "meta": {
        "feature_module": "shared",
        "tenant_configurable": false,
        "used_by": ["payments", "transactions", "reconciliation"]
    }
}
