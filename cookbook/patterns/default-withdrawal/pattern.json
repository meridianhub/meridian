{
    "$schema": "https://cookbook.meridianhub.org/schema/registry-item.json",
    "name": "default-withdrawal",
    "type": "registry:pattern",
    "title": "Current Account Withdrawal",
    "description": "Multi-step saga for withdrawing funds from a current account with double-entry ledger posting. Logs a DEBIT position in position keeping, initiates a booking log, posts DEBIT to customer account and CREDIT to clearing account, then finalizes the booking to POSTED state.",
    "registryDependencies": ["base-fiat-gbp"],
    "categories": ["banking", "ledger", "double-entry"],
    "meta": {
        "complexity": 3,
        "design_pattern": "current-account-operation",
        "industries": ["banking", "fintech"],
        "provides": {
            "instruments": [],
            "account_types": ["CURRENT", "CLEARING"],
            "sagas": ["current_account_withdrawal"],
            "valuation_rules": [],
            "triggers": ["api:/v1/accounts/{account_id}/withdraw"]
        },
        "requires": {
            "instruments": ["GBP"],
            "market_data": []
        },
        "composes_with": ["default-deposit"],
        "conflicts_with": [],
        "extends": ["base-fiat-gbp"]
    },
    "files": [
        {
            "path": "patterns/default-withdrawal/manifest-fragment.yaml",
            "type": "registry:file",
            "target": "~/manifest-fragment.yaml"
        },
        {
            "path": "patterns/default-withdrawal/current_account_withdrawal.star",
            "type": "registry:file",
            "target": "~/sagas/current_account_withdrawal.star"
        }
    ]
}
