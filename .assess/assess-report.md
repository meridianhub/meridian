# Codebase Assessment: meridian

_Generated 2026-06-04. Generated by `/assess` v1.37.0._

**Score: 7.5 / 8 - AI-Native (Exemplary)** · Keyhole: 12 structural concerns (10 lying map, 2 self referential tests), 2 safe zones.

Meridian is, on the write side, about as enforced as a Go monorepo gets: phantom-typed dimensional `Quantity[D]`, zero `unsafe`, a `.golangci.yml` that actually fences per-function complexity (worst production function sits under its own `cyclop:15` gate), an executable `tests/architecture/` suite that turns conventions into CI failures, blocking 75%/70% Codecov gates across 50+ components, two review bots on every PR, and a closed feedback loop (marathon retros, memory files, 50+ PRDs). Eight of nine layers are Present. The single gap is read-side wayfinding: 26 module READMEs sit orphaned off the doc graph and a handful of service READMEs have drifted behind code that churns under them - navigable terrain with a partly-missing map, not missing terrain. The highest-leverage next step is to reconcile and wire those few service/frontend READMEs back into the graph so an agent's first hop lands on a current map.

## Top 3 Actions

Actions 1-3 are the deterministic attention list verbatim - the three paths the cross-layer scan ranked as today's structural concerns. All three live in Layer 0/trust (the only non-full layer): two drifted "lying-map" READMEs and one same-commit test suite that needs an independent eye.

| # | Action | Layer | Effort | Command / First Step | Hotspot files this addresses | Issue |
|---|--------|-------|--------|---------------------|------------------------------|-------|
| 1 | Reconcile `frontend/README.md` against the current `frontend/` code, then wire it into the doc graph (it is also an orphan - nothing links to it) | 0 | small | Open `frontend/README.md`; diff its claims against `frontend/src/`; refresh drifted sections or delete superseded ones; add an inbound link from `README.md` / a docs index | `frontend/README.md` | TM `assess-2026-06-04` #1 |
| 2 | Request independent human review of `frontend/src/App.tsx` and its co-located tests - code and tests entered together, so the suite may pin the author's mental model rather than specified behaviour | 0 | small | Tag a human reviewer on `frontend/src/App.tsx` (66 commits, the most-churned file in the repo); confirm tests assert observable behaviour, not internal structure | `frontend/src/App.tsx` | TM `assess-2026-06-04` #2 |
| 3 | Reconcile `scripts/README.md` against the current `scripts/` directory, then wire it into the graph | 0 | small | Open `scripts/README.md`; verify it still describes the scripts that exist (`demo.sh`, `doctor.sh`, etc.); refresh or delete; add an inbound link | `scripts/README.md`, `scripts/demo.sh`, `scripts/doctor.sh` | TM `assess-2026-06-04` #3 |

### Why these three?

These are the deterministic "look here first" ranking, not a judgement call: the cross-layer scan found code that churns under a comparatively static README (the `lying_map` axis) and a file whose tests were born in the same commit as the code (`self_referential_tests`). Each is cheap to close and each removes a *confidently-wrong* signal - the worst kind for an agent, because a drifted README sends it navigating fast to a stale conclusion, and a same-commit test suite reads as "verified" while only proving the code agrees with itself. None of these is a rewrite; all three are reconcile-and-link or request-a-second-pair-of-eyes.

## Snapshots

### Complexity - riskiest to change

[![Complexity hotspot](./complexity-heatmap.svg)](./complexity-heatmap.svg)

Risk concentrates in large integration-test files (`current-account`, `payment-order`, `financial-accounting` gRPC test suites) - big and churning but tests, intentionally exempt from `funlen`/`cyclop`; the only production hotspots are `frontend/src/App.tsx` (66 commits) and `cmd/meridian/main.go` (64 commits), both high-churn but with worst-function complexity well under the enforced `cyclop:15` gate.

### Doc navigability - can an agent find its way?

[![Doc map](./doc-graph.svg)](./doc-graph.svg)

88.7% of docs are reachable from the entry points (`README.md`, `CLAUDE.md`, `AGENTS.md`); the ~11% gap is 26 orphan module READMEs - a curation/wayfinding gap, not hidden content - plus 11 docs named in prose without a link. No genuine lying maps among the central hubs.

<details>
<summary>📈 Snapshot detail (commit, hotspots, navigability, lying maps)</summary>

#### Complexity profile

- **Measured at commit:** `1b28b180933a` (2026-06-04) - **working tree had uncommitted changes; figures include un-committed edits**
- **Files scored:** 4030
- **Churn window chosen:** last 12mo
- **Complexity profile:** file-aggregate ccn p50 14, p95 75 (max 336); est. tokens p50 1,410, p95 6,298 (max 38,671); LOC p50 134, p95 598 (max 3,144). Per-function complexity is far tighter - the worst single function across all top hotspots is ccn 21 (in an integration test), and production functions stay under the `cyclop:15` gate.
- **Top hotspots** (composite `sqrt(ccn) × sqrt(1 + commits) × sqrt(est_tokens)` - a sub-linear blend of complexity, recent churn, and context-window size, so a file high on *multiple* axes leads; a frozen-but-complex file ranks below an equally-sized active one). `est_tokens` is the char-based estimate (~chars/4); `ccn` here is the **file aggregate**; the worst single function per file is in parentheses:
  1. `services/current-account/service/grpc_service_integration_test.go` - 15,362 est. tokens (1,069 LOC), aggregate ccn 98 (worst function 21), 33 commits in window
  2. `services/payment-order/service/grpc_service_test.go` - 24,924 est. tokens (2,283 LOC), aggregate ccn 204 (worst function 11), 17 commits
  3. `services/financial-accounting/service/financial_accounting_service_test.go` - 20,755 est. tokens (1,943 LOC), aggregate ccn 116 (worst function 20), 11 commits
  4. `frontend/src/App.tsx` - 5,349 est. tokens (289 LOC), aggregate ccn 91 (worst function 8), 66 commits *(the most-churned production file)*
  5. `cmd/meridian/main.go` - 4,702 est. tokens (431 LOC), aggregate ccn 69 (worst function 10), 64 commits
  6. `scripts/demo.sh` - 14,489 est. tokens (1,113 LOC), aggregate ccn 118, 6 commits

  The top three are all test files. Their high aggregate ccn is the sum of many low-complexity table-driven cases (worst single function ≤21), not tangled logic - they are exempt from `funlen`/`cyclop` by design and are not refactor targets.

- **Keyhole budget** (`stats_summary.est_tokens.budget`): repo is ~8.66M est. tokens; **0 files** exceed the ~200k single-keyhole budget, but **6 subtrees** do: `services` (~4.69M), `shared` (~1.11M), `docs` (~1.03M), `frontend` (~776k), `api` (~284k), `cmd` (~227k). No single file blows a context window; whole-subtree reads need chunking.

Size encodes estimated tokens (~chars/4 - the keyhole size unit; LOC is one hover away in the tooltip), colour encodes cyclomatic complexity (dark red = high), saturation encodes recent git churn (vivid = active). Tokens track what fills an agent's context window better than LOC. Vivid red blocks are the migration risk. No mutation overlay was run, so no hatching is present.

#### Doc navigability

- **Navigability, in words.** Of 247 docs, **88.7% are reachable** by following links from the entry points (`README.md`, `CLAUDE.md`, `AGENTS.md`); the other ~11% are **orphans** (nothing links to them) spread across **20 disconnected islands**. The 26 orphans are overwhelmingly *module* READMEs and platform docs an agent would want on its first hop into a subtree: `frontend/README.md`, `shared/README.md`, `services/market-information/README.md`, `shared/platform/observability/README.md`, `shared/platform/bootstrap/README.md`, `services/payment-order/service/HANDLER_ARCHITECTURE.md`, `shared/domain/money/PRECISION_GUARANTEES.md`, and the `services/mcp-server` CEL/Starlark reference docs. This is a **curation/wayfinding gap, not an access gap** - an agent can still `ls services/` and open any README by path; what is missing is a navigable map that *links* them. The fix is to wire these into an index/MOC and add the inbound links; for a directory-organised tree like this one, treat it as a wayfinding improvement, not a blocker.
- **Missing cross-references.** 11 docs name another doc in prose without linking it - e.g. `cookbook/README.md` mentions `authoring-patterns.md` / `authoring-components.md`; `docs/adr/0015-standard-service-directory-structure.md` names four guides (circuit-breaker-usage, incident-response, service-coupling-analysis, COVERAGE_ANALYSIS) without links; `docs/prd/049-codebase-consistency.md` names four convention guides. Adding these links would close most of the island count.
- **Lying maps (stale docs of churning code).** **stale** = days since the doc itself last changed; **subject churn** = commits in the window to the code it describes; **centrality** = how many docs point to it. The composite's top rows are the ADRs (`0004-event-schema-evolution.md`, etc.), but **these are not lying maps**: they were all updated **6 days ago** and their "subject churn" is `repo-baseline` (the whole repo's churn, a coarse proxy, `confidence: low`) - a freshly-edited central hub, not a stale one. The only docs with a *precise* subject association and real staleness are mid-tier architecture docs: `docs/architecture/service-coupling-analysis.md` (153 days stale, `parallel-docs-tree`, ratio 22), `docs/architecture/event-driven-architecture.md` (95 days), and `docs/architecture/bian-service-boundaries.md` (95 days). These are mild and low-priority but are the real refresh candidates.

Colour = staleness (vivid red = a frozen doc beside churning code = a lying map); structure = reachability (centre = entry, rim = unreachable, dashed ring = orphan); size = file length. Hover a node in the SVG (opened on its own) for its path and stats.

#### What changed since last run

_No prior run to compare against in this worktree - this is the first recorded snapshot._

</details>

<details>
<summary>📊 Full scorecard (per-layer evidence & gaps)</summary>

The two headline metrics measure **different things and are never combined.** The 0-8 score answers _"is the scaffolding in place to catch problems?"_ The Keyhole Readiness summary answers _"where is today's structural pain?"_ - a count of structural concerns and safe zones rolled up from the eight cross-layer findings. A repo can score 7/8 and still carry structural concerns (great scaffolding over churning code), or 3/8 with zero concerns. Reported side by side in the headline; never averaged.

The **What it asks** column is the question that layer answers for an agent working here - read it first; the framework name is secondary. The **Band** orders them by dependency: a read-side blind spot makes the write-side scores mean less, and the meta band only matters once the rest works.

| Layer | What it asks | Band | Status | Evidence | Gap |
|-------|--------------|------|--------|----------|-----|
| 0: Agent Instructions & Navigability | Can I build a true map of this codebase before I touch it? | read | **Partial** | **0a Instructions: Present** - `instructions_grade: A`; `CLAUDE.md` grade A (score 85, 42 positive directives, 19 path refs, bloat penalty 0); `AGENTS.md` is a correct thin alias to `CLAUDE.md` (single source of truth); `.github/claude-review-instructions.md` grade A (100); 9 skills under `.claude/skills/`; no broken refs, untracked instruction files, or sensitive content. **0b Navigability: Partial** - 247 docs, 659 edges, 88.7% reachable, 0 dangling/broken links, ADRs are strong hubs. | 26 orphan module READMEs across 20 islands; 11 prose cross-references unlinked; 3 mid-tier architecture docs 95-153 days stale. Wayfinding/curation gap, not absence. |
| 1: Runtime Legibility / Liveness | Can I see which parts are live, which need attention, and which are dead weight? | read | **Present** | `observability.rung: 3` (Reachable - the deciding rung): instrumented (OpenTelemetry, Prometheus, logrus), discoverable (50+ runbook/observability docs), **and agent-reachable** (13 runbooks with runnable queries; a dedicated `services/mcp-server` exists). Reachable *if* the agent has the cited query tools (`kubectl`, `logcli`, `stern`) in its execution context. Intra-repo dead code: `candidate_count: 0` (ts-prune clean). | Go `deadcode` absent from PATH and `staticcheck` available-not-run; static reachability cannot prove no *external* consumer. Run `staticcheck -checks U1000 ./...` to confirm. |
| 2: Code Design | Will the type-checker catch my mistakes? | write | **Present** | Go generics for domain types incl. the dimensional `Quantity[D]` phantom-type pattern (`shared/platform/quantity/`, `shared/pkg/types/`); **zero `unsafe` blocks** across the Go tree; `forcetypeassert`, `nilnil`, `errorlint`, `err113` enforced in CI. | - |
| 3: Linters | Are complexity and style bounds enforced, or will my code drift? | write | **Present** | `.golangci.yml` with enforced thresholds: `gocyclo` 15, `gocognit` 20, `funlen` 80/60, `cyclop` 15, `godox` (rejects TODO/FIXME), `depguard` (import boundaries), `exhaustive`, `nolintlint`. Per-function ccn p95 6 / max 84 across 47,402 functions; worst `top_complex` rows are all test files (exempt by design). `frontend` has `eslint.config.js`. | - |
| 4: Architecture Tests | Are the structural conventions executable, or just folklore? | write | **Present** | `tests/architecture/` suite: `layers_test.go` (layer-boundary enforcement), `structure_test.go` (doc.go / server.go naming), `naming_test.go`, `size_test.go` (800/1500-line ratchet with allowlist). Backed by `verify-service-conventions.sh`, `check-cross-service-imports.sh`, `lint-multi-asset-purity.sh`, wired into `conventions.yml` + `service-readme-lint.yml`. | - |
| 5: CI Pipeline | Does something automatically catch a bad change before it merges? | write | **Present** | 30+ workflows: `build`, `test` (sharded, `-race -coverprofile -covermode=atomic`), `conventions`, `codeql`, `security`, `e2e`, `frontend`, `proto`, `migrations`, `saga-validation`, `schema-validation`, `deploy-gate`. 2 approvals required for merge (1 for markdown-only). | - |
| 6: Coverage Gates | Do the tests constrain behaviour, or just execute lines? | write | **Present** | `codecov.yml`: project 75% (`informational: false` - blocking), patch 70% (blocking), `require_changes: true`, per-component 80% across 50+ components. Generated/mock/cmd paths correctly ignored. | **No mutation testing anywhere** (no go-mutesting/Stryker/PITest) - truth-pressure on the gates is *unverified*: high line coverage with unknown behaviour-constraint. The one place to watch for the hollow-gate pattern. |
| 7: Code Review Bots | Is there design-level feedback on every change? | write | **Present** | Two active bots: CodeRabbit (`.coderabbit.yaml`, reviews `develop` + `main`, generated files excluded) and `claude[bot]` (`claude-review.yml` + `claude.yml`). | - |
| 8: AI Project Mgmt (capstone) | Do learnings feed back into the contracts, or evaporate? | meta | **Present** | Task Master active (tags `valuation-engine`, `self-service-onboarding-readiness`; `.taskmaster/` correctly not vendored, state at parent dir per convention); structured marathon-retros log + per-feedback memory files traced to incidents; 50+ PRDs in `docs/prd/`; wave-based marathon orchestration; bot-reviewer protocols codified. | - |

**Layer 1 caveat (by default).** Layer 1 scores what the *repo* makes agent-reachable, not what the agent has installed. Rung 3 here means the runbooks fence runnable queries; an agent realises that legibility only *if* `kubectl` / `logcli` / `stern` are in its execution context.

### Score derivation (worked)

Present = 1, Partial = 0.5, Missing = 0, across 9 layers (0-8), displayed capped at 8. Meridian: L0 Partial (0.5) + L1-L8 all Present (8 × 1.0) = **8.5 raw → displayed 7.5 / 8** (the single Partial costs 0.5 against the ceiling).

### Maturity Level

| Score | Level | Description |
|-------|-------|-------------|
| 0-2 | Not Ready | Agent will produce inconsistent, unvalidated code |
| 3-4 | Basic | Norms exist but aren't enforced. Agent works but drifts |
| 5-6 | Solid | Contracts catch most issues. Agent is productive |
| 7-8 | AI-Native | System self-improves. Agents work reliably at scale |

</details>

<details>
<summary>🔎 Cross-layer findings & lying signals (keyhole detail)</summary>

### Lying Signals

These artefacts look true but aren't - the most dangerous failure mode for an agent navigating the codebase.

| Layer | Signal Type | Instance | Why it lies |
|-------|-------------|----------|-------------|
| 0 | Stale hub doc | `docs/architecture/service-coupling-analysis.md` (153d stale; precise `parallel-docs-tree` association, staleness ratio 22) | A referenced architecture doc, frozen 5 months while its subject code moved - reads as the current map of service coupling but describes an older topology. (The composite's higher-ranked ADRs were excluded: they were edited 6 days ago and only score high on coarse repo-baseline churn.) |

_L1 (dead-but-present): omitted - `candidate_count: 0`. L6 (green-but-hollow): omitted - no mutation pass was run, so survivor density is unmeasured._

### Cross-Layer Findings (Keyhole Readiness)

The deterministic core crosses the layer axes against each other and emits the named findings below. The READMEs flagged under `lying_map` are service/module docs sitting over heavily-churning service code; the action is to reconcile each against its code (refresh or delete), not to assume the doc is right. `refactor_boundary` is the positive signal - safe zones where edits stay local.

## Cross-Layer Findings (Keyhole Readiness)

### lying_map

Action: fix or delete the doc

Paths:
- frontend/README.md
- scripts/README.md
- services/control-plane/README.md
- services/current-account/README.md
- services/financial-accounting/README.md
- services/identity/README.md
- services/internal-account/README.md
- services/mcp-server/README.md
- services/position-keeping/README.md
- shared/README.md

### self_referential_tests

Action: request human review - tests verify internal consistency, not truth

Paths:
- frontend/src/App.tsx
- scripts/doctor.sh

### refactor_boundary

Action: safe to hand an agent in isolation

Paths:
- docs/prd
- frontend

### Attention List (Priority Order)

- frontend/README.md (score 1): lying_map
- frontend/src/App.tsx (score 1): self_referential_tests
- scripts/README.md (score 1): lying_map
- scripts/doctor.sh (score 1): self_referential_tests
- services/control-plane/README.md (score 1): lying_map

</details>

<details>
<summary>✅ Strengths & further opportunities</summary>

### Strengths

- **Correctness by construction.** Phantom-typed dimensional `Quantity[D]` (`shared/platform/quantity/`) makes mixing kWh and GBP a *compile error*, and the Go tree carries **zero `unsafe` blocks** - the type system is doing load-bearing work, not decoration.
- **The complexity gate actually bites.** `.golangci.yml` enforces `cyclop:15` / `gocognit:20` / `funlen:80` and the data proves it holds: per-function ccn p95 is **6** across 47,402 functions, and every high-aggregate file in the hotspot list is a table-driven *test*, not tangled production logic.
- **Conventions are executable, not folklore.** `tests/architecture/` (layer boundaries, naming, file-size ratchet) plus three convention shell-linters are wired into CI - structural drift fails the build.
- **Honest, blocking coverage gates** (`codecov.yml`, 75%/70% non-informational, 80% per-component across 50+ components) with generated paths correctly excluded.
- **The feedback loop is closed (Layer 8).** Marathon retros, per-incident memory files, 50+ PRDs, and codified bot-reviewer protocols mean learnings feed back into the contracts instead of evaporating - the capstone most repos never reach.
- **Safe refactor zones** identified: `docs/prd` and `frontend` show high edit-containment - safe to hand an agent in isolation.

### Additional Opportunities

_After the Top 3:_

- **Wire the 26 orphan docs into the graph.** Add inbound links / an index entry for `shared/README.md`, `services/market-information/README.md`, `shared/platform/observability/README.md`, `services/payment-order/service/HANDLER_ARCHITECTURE.md`, `shared/domain/money/PRECISION_GUARANTEES.md`, and the `services/mcp-server` CEL/Starlark reference docs. Add the 11 missing prose cross-references (one-line link edits in `cookbook/README.md`, `docs/adr/0015-...`, `docs/prd/049-...`).
- **Refresh the 3 genuinely-stale architecture docs:** `docs/architecture/service-coupling-analysis.md` (153d), `event-driven-architecture.md` (95d), `bian-service-boundaries.md` (95d).
- **Add mutation testing to the financial-correctness core** to convert Layer 6 from "covered" to "behaviour-pinned" - run `go-mutesting` (or a maintained equivalent from the Go ecosystem) against `services/position-keeping` and the saga engine, where surviving mutants would mean money moves wrong.
- **Confirm the clean Go dead-code result:** `staticcheck -checks U1000 ./...` (available but not run in this read-only pass; `deadcode` was absent from PATH).
- **Contradiction check (out of deterministic scope):** with 247 interlinked docs, run an LLM over `docs/` to flag pages that contradict each other or the code - `/assess` does not check this.

</details>

<details>
<summary>🧭 How to read this report (framing & method)</summary>

**What this is ultimately measuring.** When you hand work to an AI here, does it behave like a brand-new hire or like an engineer who has been in the org eighteen months? The difference isn't capability - it's *externalized context*: knowing where things live, which contracts are load-bearing, where the minefields are. An AI contributor is structurally always the new hire - it starts every session with an empty head, seeing one narrow slice through its context window - so this report scores how much of the tenured engineer's implicit map the codebase has made explicit and navigable. The aim is not an AI that comprehends what humans no longer can and is trusted blindly; it is a codebase legible enough that the relevant slice fits one context window and the agent's answers stay anchored to code you can still verify. **Legibility you can trust, not omniscience you can't.** The same ethic points at the write side: the linters, architecture tests, CI gates, coverage, and review automation read not as a leash on the agent but as the guardrails that protect any contributor, human or AI, from costly mistakes by design.

This is an improvement roadmap, not a verdict. It pairs three views:

- **Where the codebase is today** - the complexity heatmap. Vivid red = complex AND actively changing = the files most likely to bite next week.
- **Whether an agent can navigate it** - the doc graph: how much is reachable from the entry point, and which docs are stale maps of churning code.
- **What keeps it from getting worse** - the AI Readiness score (0-8) across read-side foundation, write-side enforcement, and meta.

A codebase can be 8/8 and still on fire, or 2/8 with a calm treemap. The views matter together.

**The legacy-transition lens.** The hard part of AI-assisted coding is the same one the industry has faced for 25 years: what happens when the people who understood the code are gone, or the code outruns anyone's ability to understand it? AI only changes the *velocity*. Read through accumulated legacy-code discipline:

- **"Legacy code is code without tests."** (Feathers). A complex, churning, untested file is already legacy. The response is *characterization tests* (pin actual current behaviour) plus a *seam*, then refactor.
- **Don't rewrite - strangle** (Spolsky / Fowler's Strangler Fig). An agent's default ("regenerate from intent") discards undocumented behaviour and accreted bug-fixes - usually the wrong move under AI velocity.
- **Hotspots and change-coupling are the map** (Tornhill / CodeScene). The complexity-×-churn heatmap *is* hotspot analysis; treat vivid-red blocks and coupling seams as the worklist.
- **Decide per component (the 7 Rs):** retain, retire, rehost, replatform, refactor, re-architect, rebuild. Dead weight → retire; complex-and-live → characterize then refactor; safe `refactor_boundary` zones → hand to an agent in isolation.

**How it's measured.** An AI-readiness review run almost entirely on *traditional* tooling - static analysis, git history, graph metrics over docs and code. The model only writes the prose around those numbers; it does no scanning itself. That keeps a run fast, near-zero in model tokens, and reproducible run-to-run.

</details>

<details>
<summary>🤖 Machine-readable data (for agents)</summary>

If you are an agent working in this repo, the `.assess/` directory is actionable feedback written for you:

- `.assess/assess-report.md` - this report.
- `.assess/run-context.json` - the full data bus (findings, attention, keyhole summary, prescribed actions, stats, diff).
- `.assess/complexity-stats.json` - complexity / estimated-token / LOC percentiles, the keyhole-budget rollup, and the ranked file lists.
- `.assess/hotspots/<file>.md` - per-file briefings, each with a **Suggested actions** section. Read a file's briefing before you change it.
- `.assess/index.md` - the catalog of every hotspot ever flagged.
- `.assess/log.md` - append-only run history.

</details>

---

_Report generated by [`/ai-native-toolkit:assess`](https://github.com/bjcoombs/ai-native-toolkit). Install in any Claude Code session: `/plugin marketplace add https://github.com/bjcoombs/ai-native-toolkit` then `/plugin install ai-native-toolkit@ai-native-toolkit`._
