coverage:
  status:
    project:
      default:
        target: 50%
        threshold: 2%
        informational: true
    patch:
      default:
        target: 60%
        informational: true

ignore:
  - "**/*.pb.go"
  - "**/*.pb.validate.go"
  - "**/*_grpc.pb.go"
  - "**/mocks/**"
  - "utilities/**"
  - "frontend/src/api/gen/**"

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  behavior: default
  require_changes: true

flags:
  unittests:
    paths:
      - "services/"
      - "shared/"
    carryforward: true
  integration:
    paths:
      - "services/"
      - "shared/"
    carryforward: true
  frontend:
    paths:
      - "frontend/src/"
    carryforward: true
