# Port Configuration: See shared/platform/ports/ports.go for canonical port assignments.
# This service uses ports.Gateway for HTTP health/metrics endpoints.
apiVersion: v1
kind: Service
metadata:
  name: utilization-metering-consumer
  labels:
    app: utilization-metering-consumer
    component: consumer
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    app: utilization-metering-consumer
