# Port Configuration: See shared/platform/ports/ports.go for canonical port assignments.
# This service uses port 8090 for HTTP transport (MCP_PORT).
apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  labels:
    app: mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8090
    targetPort: http
    protocol: TCP
  selector:
    app: mcp-server
