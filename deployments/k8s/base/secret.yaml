# SECURITY NOTE: This secret contains development credentials only.
# For production deployments:
# - Use external secret management (e.g., Sealed Secrets, External Secrets Operator)
# - Rotate credentials regularly
# - Use RBAC to restrict secret access
# - Enable encryption at rest for secrets
apiVersion: v1
kind: Secret
metadata:
  name: meridian-db
  labels:
    app: meridian
type: Opaque
stringData:
  # Local development connection string - CockroachDB in insecure mode
  # Using root user (no password) for local development
  DATABASE_URL: "postgres://root@cockroachdb:26257/defaultdb?sslmode=disable"
